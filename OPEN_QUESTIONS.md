# Open Questions

All active open questions for the current documentation-first phase are closed.

Closures are recorded in `docs/research/open-question-closure-log.md` and are backed by normative spec text, conformance criteria, policy mapping, prototype acceptance criteria, and JSON fixtures under `fixtures/`.

Future work is tracked in `BACKLOG.md` as non-blocking research, standards review, conformance automation, and prototype implementation work. Future items MUST NOT reopen the accepted baselines:
- standards-aligned architecture
- root credential held wallet-side
- derived proof as the normal verifier-facing artifact
- distinct `Profile R` and `Profile P`
- correlation as the main systems risk
- no per-presentation issuer callback in normal flow
- profile-plus-conformance as the project model
